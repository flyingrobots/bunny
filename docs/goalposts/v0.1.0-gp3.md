# Goalpost v0.1.0-GP3: Workspace Infrastructure and Code Quality Gates

## Description

Establish workspace-wide standards, linter gates, and cross-platform verification pipelines.

## Status

Complete

This is a historical goalpost document. References to
`.github/workflows/ci.yml` describe the workflow used when the goalpost was
completed; current verification runs through `.github/workflows/code-dojo.yml`.

## Acceptance Criteria

* `CODE_STANDARDS.md` created, establishing line length limits and Clippy pedantic/nursery denies.
* GitHub Actions CI pipeline (.github/workflows/ci.yml) checks Ubuntu, macOS (macos-latest), and Windows targets.
* Target `wasm32-unknown-unknown` compilation is verified on the Ubuntu runner in CI (`wasm-check` job).
* Code formatting verified via `cargo fmt --all -- --check`.
